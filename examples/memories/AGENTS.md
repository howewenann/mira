# Example project instructions

Copy this file to `.mira/memories/AGENTS.md`, then replace this text with the
project's commands, architecture, conventions, and working preferences.

Using the name `AGENTS.md` replaces MIRA's bundled default of the same name.
Choose a different Markdown filename when the guidance should be added instead.
