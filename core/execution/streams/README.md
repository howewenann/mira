# Purpose

Interpret ordered native stream channels and provider payloads.

## Owns

Message, tool, subagent, rubric, correction, and metadata projection.

## Does not own

Consumer rendering or external interoperability.

## Depends on

Native LangChain/LangGraph structures and Core context helpers.

## Public surface

Consumed internally by `core.execution.runner`.
